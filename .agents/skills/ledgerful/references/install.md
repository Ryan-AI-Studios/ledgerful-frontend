# Ledgerful Installation

If Ledgerful is not installed or not on the system `PATH`, follow these instructions.

## Linux / macOS (Bash)

```bash
curl -fsSL https://raw.githubusercontent.com/UnlikelyKiller/Ledgerful/main/install/install.sh | sh
```

## Windows (PowerShell)

```powershell
iwr https://raw.githubusercontent.com/UnlikelyKiller/Ledgerful/main/install/install.ps1 -UseB | iex
```

After installation, open a new terminal window or refresh your environment variables to ensure `ledgerful` is available.
